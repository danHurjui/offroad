# deps
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
/prisma/dev.db

# uploaded user content (dev only — served from disk, not committed)
/public/uploads/*
!/public/uploads/.gitkeep

# Scratch state written by the live E2E harness (throwaway test accounts and
# row ids handed between suites). The harness writes it next to itself now,
# but an older copy run from the project root would land it here.
/ctx.json
